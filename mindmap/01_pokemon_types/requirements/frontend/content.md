Create TypeMatrix component with 18x18 grid
Implement type filter dropdown with multi-select
Use CSS variables for type colors (--color-fire, --color-water, etc.)
Add keyboard navigation for accessibility
