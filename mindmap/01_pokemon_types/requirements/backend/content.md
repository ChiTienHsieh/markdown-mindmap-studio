Design type_effectiveness table: attacker_type, defender_type, multiplier
Create GET /api/types endpoint returning all types with colors
Create GET /api/types/{type}/pokemon endpoint for filtering
Add caching for type effectiveness lookups
